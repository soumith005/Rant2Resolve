
// AI features removed as per Rant2Resolve project requirements.
export const enhanceIssueDescription = async (title: string, roughDescription: string): Promise<string> => {
    return roughDescription;
};

export const summarizeAnnouncements = async (announcements: any[]): Promise<string> => {
    return "AI summary features disabled.";
};
